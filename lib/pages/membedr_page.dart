import 'package:flutter/material.dart';

class MembedrPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Scaffold(
        appBar: AppBar(
          title: Text('MembedrPage'),
        ),
        body: Center(
          child: Text('MembedrPage'),
        ),
      ),
    );
  }
}